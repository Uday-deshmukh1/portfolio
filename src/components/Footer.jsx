function Footer() {
  return (
    <footer className="relative z-10 border-t border-white/[0.06] bg-ink/90">
      <div className="container-grid flex flex-col justify-between gap-3 py-8 text-sm text-slate-500 sm:flex-row">
        <p>If my work caught your attention, let's start a conversation. im uday desmukh x ishant deskmukh</p>
      </div>
    </footer>
  );
}

export default Footer;
